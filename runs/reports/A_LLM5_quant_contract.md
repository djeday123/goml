# A-LLM-5: квант-контракт O(1) — внедрён, в камне, пересертифицирован (2 хвоста)

**Дата:** 2026-08-03. **Коммиты:** goml `c4a8c2e`, gotorch `927c795`, fa-blackwell-fp8 `7d2e9a0`.
**Старт:** HEAD `a530ad9`, Б-0 PASS (красный первого прогона — артефакт cwd, прибор).
**Raw:** `runs/reports/raw_allm5/` (7 логов). Канарейка: before 653.68T / after 654.02T.

## П.0 — механизм Н4 подтверждён чтением (file:line)

- **fwd v121r-train:** QK-MMA `mma...f16.e4m3.e4m3.f16` (`_v121r_train_kernel_full.cu:87-94`,
  использование :367-427) — F16-аккумулятор, узел Н4. O(PV)-MMA тот же хелпер (:634,:665) —
  безопасен при P∈[0,1], |Vd|≤1. **Второй F16-узел:** scale конвертируется в half2 (:451-452,
  fs=scale·log2e) — fs обязан быть FP16-normal [6.1e-5, 65504]. Running stats — float.
- **bwd:** merged — F32-акк (fa_bwd_merged_v1.cu:43); dk — F32 (:51,:66); **dq — F16-акк**
  (fa_bwd_dq_new.cu:47, packed). Границы dq: наивная Σ|dS|·sl·|Kd| → запас 12.5×;
  с P-нормировкой (Σ_j P_ij=1 ⇒ Σ_j|dS_ij| ≤ max|dP|+|D| ≈ 2.6) → ~25600×.
  **Вердикт-стоп dk/dq НЕ сработал** (обе границы ≥ 8×).
- dS-квант в merged: direct-cast e4m3 без скрытого масштаба; порог 2^-9/2^-6 kernel-units.

## П.1 — конвенция внедрена сквозь стек

scale_X = amax_X (Unit-квантизатор `quantize_f32_to_f8e4m3_unit`, gotorch `927c795`;
PTX-ASCII catch поймал кириллицу в комментариях первой версии — durable-правило
сработало на авторе). faScale = softmax·amaxQ·amaxK; scale_dq/dk по алгебре A-LLM-4.
Прогноз границы: |S_acc| ≤ 128, запас 512×. Цена: порог стирания amax·2^-9 (+8.8 биннад,
территория warmup). Прогноз точности (1е): подтверждён — floor'ы не хуже (см. 6г).

## П.2 — контракт в камне

Док-строки: fa_sm120.h (обе копии), fa_backward.go (+карта аккумуляторов).
**КОНТРАКТ-ТЕСТ `TestFAContract_FullRangeInputs` PASS** обе формы {bh4/sl2048, bh128/sl8192}:
O/L/D/dV/dK/dQ живые, L в прогнозной границе (7.64≤18.9; 9.03≤20.3).
Негатив (log-only, датирован): full-range decoded → O bad 1048576/1048576, L 8192/8192 —
ловушка исполняемо задокументирована. **Чувствительностная проба** (2в): возмущение
строки V → max|dO| = 6.6e-4 > 0 — attention жив. П.3: сноска Stage-3, durable-правило
[[feedback-lnv-smoke-weak-gate]], пометки G1/G2 — в хронике/шапках. П.4: Н3-Sync в
боевом fwdBattleA вписан.

## П.6 — пересертификация

- 6а/6б: G1 L-cert PASS (rel ~2-3e-4, асимметричные формы), fingerprints 38/252/124/69 OK,
  canonical chain PASS **без паники** (LockOSThread-фикс харнеса).
- **6г: первая ЧИСЛОВАЯ сертификация цепочки vs CPU-F64** (расширение D-ref, форма bh1/sl128):
  O 3.2e-3, L 3.7e-4, D 1.1e-4, **dV 7.8e-7, dK 2.1e-4, dQ 2.4e-4** — 6/6 в floor 5e-3.
- **6д: Stage1 A/B PASS по букве ТЗ** (raw fa_stage1_final.log): chain-FA жив (скейлы
  2.11/2.09/2.16, NaN=0), PRIMARY vs F64 10/10 PASS; dQ/dK-нули цепочки = легитимная
  зона B (dS_kernel ≈ 4.5e-6 << 2^-9 — стирание по контракту, cold-start класс,
  лекарство warmup). P1 fwd-sanity δ=2.3e-7.

## Два хвоста (задокументированы, НЕ чинились в хвосте окна — стоп-линия)

- **Хвост-а:** amax-гонка добита в chain-вызове (скейлы реальные), но в standalone-вызове
  блока amax=0 повторился (скейлы fallback 1.0) — гонка жива в одном из путей несмотря
  на Sync; следствие: пороги зон в standalone-логе от fallback-скейлов (мусорные числа
  T в логе), block-A/B standalone деградировал до вырожденных входов.
- **Хвост-б: П.5-гейт НЕ пройден.** Де-wrapper BH>1 внедрён (6 вызовов → plainMatMulF32),
  но recon dQ/dK остались exact-zero (FA-vs-recon = 0.000 exact; recon|Δ|(Wq) = |f64|):
  канал plain-матмула на батч-СЛАЙСАХ (gpuToHost/MatMul от sliceStore) тихо нулевой.
  SECONDARY-арбитр на attn-цепи остаётся хромым. Расследование канала — следующая сессия.

## Канарейка и регрессия

654.02T: **WITHIN ТЗ-коридора [652,656]**; скриптовый [650,654] формально превышен
на +0.02T. Разбор: дельта сессии +0.34T в шуме дня (653.68..654.02); FA-.so не
пересобирались (только gotorch-PTX — механизма влияния нет). Вилка коридоров
script-vs-ТЗ — на перецентровку отдельным решением. Регрессия: BwdCertF64, F64Ref_Unit,
MatmulPlainT, ABF32vsF64_N5, Fingerprints — все PASS.

## Статус и следующая сессия

Этап 1 (6д) — PASS по букве; Этап 2 НЕ начат (стоп-линия: два хвоста + конец окна).
A-LLM-6: (1) хвост-б — расследование slice-канала (подозрение: ToDevice(CPU0, sliceStore)),
оживление SECONDARY, гейт П.5; (2) хвост-а — добить amax-гонку standalone (кандидат:
Sync не покрывает stream копий uploadInto-fallback); (3) Этап 2 по рамкам ТЗ A-LLM-4
(2.1-2.5). Опция-Б (decoded≤4, запас 32×) — после.
