# A-LLM-6: хвосты закрыты (оба — фантомы/уже мертвы), Этап 2 — красный смоук, стоп-линия

**Дата:** 2026-08-03. **Старт:** HEAD `05f2e31`, Б-0 PASS (включая новую staged-проверку).
**Raw:** `runs/reports/raw_allm6/` (6 логов). Канарейка: до 654.16T (старый скрипт) /
654.22T (перецентрованный якорь) / после 654.xx WITHIN [652,656].

## П.1 хвост-б: ЗАКРЫТ — диагноз-фантом

Проба П.1а: все три операции slice-канала ЖИВЫ (gpuToHost/MatMul/Copy от sliceStore —
max|Δ|=0.000e+00; slice_diag.log). Подозрение (класс Н1) ОПРОВЕРГНУТО. Zero-счётчики
Stage1: recon dQ/dK **живы (zero=0/1048576)** — де-wrapper A-LLM-5 работал; вывод
A-LLM-5 «recon мёртв» был ложноположительным (совпадение worst-цифр при FA-нулях
от amax=0). ГЕЙТ П.5 ПРОЙДЕН: SECONDARY-арбитр здоров, FA-vs-recon = 1.0e-4 (живые
расхождения). Фикс не потребовался.

## П.2 хвост-а: ЗАКРЫТ — гонка уже убита Н3-Sync'ом A-LLM-5

Репро-цикл 10x в нагруженном контексте: **0/10 мёртвых, amax == host-ref побитово**
(amax_race_repro.log). Два числа: прогноз >=1/10, факт 0/10. Цикл остаётся постоянной
исполняемой пробой (TestALLM_AmaxRaceRepro).
NB: в 4б-смоуке FA-вызов боевой обвязки дал скейлы [1,1,1] — amax=0 живёт в ЕЩЁ
ОДНОМ паттерне (боевая bwd-цепочка перед блоком) — материал A-LLM-7.

## П.3: faScale FP16-normal — в трёх док-блоках + проверка в контракт-тесте. Выполнено.
## П.5: канарейка перецентрована [652,656] (скрипт с датой-комментарием), якорь 654.22T.

## П.4 Этап 2: обвязка написана, смоук КРАСНЫЙ — стоп-линия

4а: BattleASnapScratch (отдельный scratch), fwdBattleA со снапшотами, bwdBattleA
переведён на снапшот-математику (v1-упрощения удалены: recompute Normed1/Normed2/
FFN-цепочки, Q_buf из snap.OAttn, RMSNormGrad'ы на XPre*), case AttnBwdFA =
сертифицированный attnFABwdBlock, таймеры kernels/staging (раздельная атрибуция).

4б смоук (stage2_interpath.log): **7/10 FAIL**. DWout/DNormOut/DWq — идеальны
(до 3.7e-9/0.000); DWo 0.166 (флор 0.13 — дрожь-класс, пограничен); но DEmbed 18.7,
DWv 0.18, DW1 0.057, DNorm1/DNorm2 0.03 — СИСТЕМАТИКА (блоу-ап класс, не дрожь).
Паттерн для A-LLM-7: top-часть и dWq-ветка сходятся идеально, dX-цепочка
(RMSNormGrad-аккумуляция -> dEmbed) разъезжается. Плюс рецидив amax=0 в FA-вызове
именно этой обвязки. Траектория/скорость НЕ гонялись (числа на несертифицированной
обвязке = мусор). Тесты Stage2 — known-red за env GOML_STAGE2=1.

## Гигиена

Регрессия ВСЯ зелёная: BwdCertF64, F64Ref_Unit, MatmulPlainT, ABF32vsF64_N5,
AmaxRaceRepro, SliceChannelDiag, Stage1, Contract, ChainVsCPURef, Fingerprints.
Инцидентов staged нет (проверки перед каждым коммитом).

## A-LLM-7 (скелет): диагностика 4б-систематики

1. Изолировать слом dX-цепочки: D-hop сверка bwdBattleA vs bwdBattleAF32 позвенно
   (dNormedTop -> dX-after-top -> dFFN* -> dNorm2-dx -> dAttnOut -> dNorm1-dx -> dEmbed)
   на общих входах — найти первое расходящееся звено. Кандидаты: порядок
   residual-аккумуляции dX в v1-скелете (bwdBattleA FFN-блок писал dFFNSilu из
   bs.DFFNOut ДО recompute — сверить порядок с эталоном), RMSNormGrad2 вход.
2. amax=0 паттерн боевой обвязки: репро с предшествующей bwd-цепочкой (не голыми
   матмулами — П.2а-цикл был слишком чистым).
3. После зелёного 4б: траектория + скорость + карта (код готов, снять env-гейт).
