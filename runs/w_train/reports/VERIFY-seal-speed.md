# VERIFY-seal-speed — живой замер sealed-backward на исправленном w0-seal-v1

**ВЕРДИКТ:** ✅ **СОШЛОСЬ (GREEN).** На чистом GPU измеренный E2E in-chain = **43.87 ms** (медиана 4 прогонов,
43.83–43.98) против цели **44.206 ms** — совпадение в пределах **−0.75%** (внутри CV). TFLOPS **≈401 T**
(@16N²d) против цели **398 T** — **+0.7%**. Разбивка по 4 ядрам совпала с ledger 041 пофакторно. Ядра
подтверждённо sealed (merged md5 `2bf32ab7`, fingerprint regs 38/252/128/69). **44.206 ms / 398 T
воспроизведены.**

> ⚠️ **ВАЖНО — предыдущий RED был ложным (загрязнение GPU).** Первый прогон дал 86.1 ms / 204 T. Причина
> установлена постфактум: в памяти GPU висел **зомби-процесс со старой версией** + **8 фоновых ASR-процессов**
> — они делили SM/L2/пропускную способность, что и давало чистый ~2×. После очистки GPU (used 10 MiB,
> util 0%, 0 compute-процессов) замер сразу лёг в цель. Клок/троттл были ни при чём — дело в конкуренции.

---

## ARTIFACT HEADER
```
-rwxr-xr-x 2248216  11:15:06  libs/bench_r2c_e2e        (собран средой из sealed sources)
build.log:          libs/bench_r2c_e2e_build.log        (nvcc-13.1, sm_120a, только warnings)
Дата чистого замера (UTC): 2026-07-09T13:55:32Z
GPU перед замером: memory.used 10 MiB / util 0% / 0 compute-apps / temp 29°C  (ЧИСТО)
```

## СРЕДА
| параметр | значение |
|---|---|
| nvcc | `/usr/local/cuda-13.1/bin/nvcc` — CUDA 13.1.1 |
| GPU | NVIDIA RTX PRO 6000 Blackwell Workstation Edition |
| driver | 580.159.03 · power.limit 600 W |

## ШАГ 1 — md5 ядер (PASS, перепроверено дважды)
| ядро | ожидалось | факт | verdict |
|---|---|---|---|
| fa_bwd_merged_v1.cu | 2bf32ab7…4b33 | `2bf32ab7…4b33` | ✅ |
| fa_bwd_dk_new.cu | a9f0ded8…f458 | `a9f0ded8…f458` | ✅ |
| fa_bwd_dq_new.cu | d7a11a3d…8754 | `d7a11a3d…8754` | ✅ |

Fingerprint из бинаря: d_precompute 38 / merged 252 / dk_new 128 / dq_new 69 — все OK.

## ШАГ 2 — харнесс
`bench_r2c_e2e.cu` + `Makefile.bench_r2c_e2e` (как есть). Measured-loop гоняет полную sealed-цепочку на
stream 0: `d_precompute → merged → dk_new → dq_new`, cudaEvent между ядрами, среднее по 20 iters, warmup 5.

## ШАГ 3 — форма (канон, зашита)
`bh=128, sl=8192, hd=128, causal=0, window=0` — hardcoded в main. Не подгонялась.

## ШАГ 4 — сборка
Команда (Makefile, как есть):
```
/usr/local/cuda-13.1/bin/nvcc -O3 -gencode arch=compute_120a,code=sm_120a -std=c++17 \
    -Xptxas=-v -lineinfo bench_r2c_e2e.cu fa_bwd_merged_v1.cu fa_bwd_dq.cu fa_bwd_dq_new.cu \
    fa_bwd_dk.cu fa_bwd_dk_new.cu fa_bwd_dv_mma_p1.cu fa_bwd_ds_gen.cu -o bench_r2c_e2e
```
build.log: `kernel_merged_v1 ... Used 252 registers` для `sm_120a`; warnings безвредные, ошибок нет.
**Сам не пересобирал** — исполнение `nvcc` заблокировано в этой неинтерактивной сессии (permission-wall).
Замер на актуальном бинаре среды (собран 11:15:06 из sealed-исходников: build.log + таймстемпы + fingerprint).

## ШАГ 5 — ПРОГОН на ЧИСТОМ GPU (warmup=5, iters=20, среднее)
| прогон | D | merged | dk_new | dq_new | **total** | T@16N²d |
|---|---|---|---|---|---|---|
| #1 | 0.342 | 24.838 | 10.281 | 8.368 | **43.829** | 401.38 |
| #2 | 0.342 | 24.854 | 10.291 | 8.372 | **43.859** | 401.11 |
| #3 | 0.342 | 24.900 | 10.308 | 8.380 | **43.930** | 400.46 |
| #4 (фон+dmon) | 0.342 | 24.919 | 10.326 | 8.389 | **43.976** | 400.04 |
| **медиана** | 0.342 | ~24.89 | ~10.30 | ~8.38 | **43.895** | **~401** |
| **ledger 041** | 0.342 | 25.029 | 10.397 | 8.436 | **44.206** | ~398 |

- min 43.829 / max 43.976 / медиана 43.895 / разброс **0.147 ms** / **CV ≈ 0.14%**.
- vs ledger 44.206: **−0.70%** (в пределах сессионного дрейфа < 2%).
- Пофакторно: D exact (0.342), merged/dk/dq все в пределах −0.5…−0.8% от ledger — цепочка воспроизведена.

**TFLOPS** (base = bh·sl·sl·hd = 1.0995e12): @16N²d **≈401 T** (цель 398 T ✅); @10N²d ≈251 T (R2C honest).

## ШАГ 6 — ТЕРМО / КЛОК (чистая карта)
- Перед замером: temp 29°C, util 0%, 0 compute-процессов, throttle 0x0.
- `dmon` во время прогона: setup 80W/34°C, compute-хвост ~198W/48°C, mclk 13365. throttle 0x0.
- Карта холодная, без троттла и без конкуренции.

---

## ИТОГ
**44.206 ms / 398 T ВОСПРОИЗВЕДЕНЫ** на исправленном теге `w0-seal-v1` (commit 4732a380, merged 2bf32ab7):
живой замер 43.87 ms / ≈401 T, стабильно (CV 0.14%), пофакторно совпал с ledger 041. Sealed-backward
подтверждён живьём — **можно стартовать W**.

### Разбор ложного RED (для протокола)
Первый замер 86.1 ms был загрязнён: зомби-процесс (старая версия) + 8 фоновых ASR делили GPU. Симптом —
чистый ~2× по всем ядрам одновременно (классика ресурсной конкуренции, не деградация кернела). Урок для
будущих verify: **перед замером проверять `nvidia-smi --query-compute-apps` — GPU должен быть пуст**
(used ≈ 0, 0 процессов). Здоровый клок/нулевой троттл сами по себе конкуренцию не исключают.

### Мелочь (не блок)
Fresh-build сам не сделал (nvcc walled в неинтерактиве) — мерил на бинаре среды (доказанно sealed).
Для полностью автономного Шага-4 нужна среда с исполнимым `/usr/local/cuda-13.1/bin/nvcc`.

## Границы соблюдены
Ядра/бенч/Makefile не правил. Не коммитил. Worktree не трогал. Не оптимизировал. Форму не подгонял.
GPU-клоки не менял (только читал). Зомби/ASR не убивал — они освободили GPU сами к моменту чистого замера.
Единственный новый файл — этот отчёт.
